---
interface Props {
  badge?: string;
  headline?: string;
  subheadline?: string;
  cta?: string;
  promoLine?: string;
}

const {
  badge = '✨ Tu asistente por WhatsApp',
  headline = '¿Quieres crecer tu estética o barbería?<br />Conoce a <span class="gradient-text">Parlo</span>, el asistente personal que siempre quisiste',
  subheadline = 'Tu asistente 24/7 que reserva a tus clientes, gestiona y actualiza tu agenda, manda promociones y te ayuda a que tus clientes regresen con recordatorios personalizados — <strong>todo desde WhatsApp</strong>. Y nunca se va a desaparecer en su hora de comida.',
  cta = 'Únete a la lista de espera exclusiva',
  promoLine = '🎁 Primera versión <strong class="text-secondary">totalmente gratis</strong> para grupo exclusivo',
} = Astro.props;
---

<section class="py-20 px-8 pb-12 text-center bg-gradient-to-b from-primary/[0.04] to-transparent">
  <div class="max-w-[1200px] mx-auto">
    <div class="inline-flex items-center gap-2 bg-gradient-to-br from-primary/[0.08] to-secondary/[0.08] border-[1.5px] border-primary/30 px-5 py-2 rounded-full font-semibold text-sm text-primary mb-6 opacity-0 animate-fade-in">
      <span set:html={badge} />
    </div>

    <h1 class="font-heading text-[clamp(2.5rem,6vw,4.5rem)] font-extrabold leading-[1.1] mb-6 opacity-0 animate-fade-in-delay-1" set:html={headline} />

    <p class="text-[clamp(1.1rem,2.5vw,1.35rem)] text-text-light max-w-[700px] mx-auto mb-6 opacity-0 animate-fade-in-delay-2" set:html={subheadline} />

    <p class="text-lg text-primary font-semibold mb-8 opacity-0 animate-fade-in-delay-3" set:html={promoLine} />

    <div class="flex justify-center opacity-0 animate-fade-in-delay-4">
      <a
        href="#form-section"
        class="px-10 py-5 gradient-bg text-white rounded-xl font-bold text-lg font-heading transition-all duration-300 shadow-[0_8px_25px_rgba(124,58,237,0.3)] hover:-translate-y-1 hover:shadow-[0_12px_35px_rgba(124,58,237,0.4)]"
      >
        {cta}
      </a>
    </div>
  </div>
</section>
