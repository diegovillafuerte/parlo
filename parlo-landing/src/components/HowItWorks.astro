---
import Carousel from './Carousel.astro';
import type { SlideData } from '../data/slides';

interface Props {
  subtitle?: string;
  slides?: SlideData[];
}

const {
  subtitle = 'Cinco formas en que Parlo transforma tu negocio',
  slides,
} = Astro.props;
---

<section class="py-20 px-8 bg-white overflow-hidden">
  <h2 class="font-heading text-[clamp(2rem,4vw,3rem)] font-extrabold text-center mb-4 reveal">
    Cómo funciona <span class="gradient-text">Parlo</span>
  </h2>
  <p class="text-center text-text-light text-lg mb-12 max-w-[600px] mx-auto reveal">
    {subtitle}
  </p>

  <div class="reveal">
    <Carousel slides={slides} />
  </div>

  <!-- CTA under carousel -->
  <div class="text-center mt-12 reveal">
    <a
      href="#form-section"
      class="inline-block px-10 py-5 gradient-bg text-white rounded-xl font-bold text-lg font-heading transition-all duration-300 shadow-[0_8px_25px_rgba(124,58,237,0.3)] hover:-translate-y-1 hover:shadow-[0_12px_35px_rgba(124,58,237,0.4)]"
    >
      Únete a la lista de espera exclusiva
    </a>
  </div>
</section>
